import styled from "styled-components";

export const FloatingKeyboardContainer = styled.div`
  background-color: ${(p) => p.theme.colors?.background?.secondary};
  padding: 10px 0;
  border-radius: 16px;
`;
