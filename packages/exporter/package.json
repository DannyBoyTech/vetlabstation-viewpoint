{
  "name": "@viewpoint/exporter",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "clean": "rimraf node_modules",
    "dev": "cross-env NODE_ENV=development nodemon --watch 'src/**/*.ts' --exec ts-node src/index.ts",
    "export": "ts-node src/index.ts",
    "build": "rimraf dist && tsc && esbuild src/index.ts --platform-node --bundle --minify --outfile=dist/index.js",
    "tsc": "tsc"
  },
  "devDependencies": {
    "@tsconfig/node18-strictest": "^1.0.0",
    "@types/node": "18.19.41",
    "cross-env": "7.0.3",
    "nodemon": "2.0.19",
    "rimraf": "^3.0.2",
    "ts-node": "10.9.1",
    "typescript": "4.8.2"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "winston": "^3.13.1"
  }
}
